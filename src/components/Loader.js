import React from 'react';

const Loader = () => {
    return (
        <div className="meinContainer loaderContainer">
            <span className="loader"></span>
        </div>
    );
};

export default Loader;